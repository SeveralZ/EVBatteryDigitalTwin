import React from 'react';
import classNames from 'classnames';
import { SectionSplitProps } from '../../utils/SectionProps';
import SectionHeader from './partials/SectionHeader';
import Image from '../elements/Image';

const propTypes = {
  ...SectionSplitProps.types
}

const defaultProps = {
  ...SectionSplitProps.defaults
}

const FeaturesSplit = ({
  className,
  topOuterDivider,
  bottomOuterDivider,
  topDivider,
  bottomDivider,
  hasBgColor,
  invertColor,
  invertMobile,
  invertDesktop,
  alignTop,
  imageFill,
  ...props
}) => {

  const outerClasses = classNames(
    'features-split section',
    topOuterDivider && 'has-top-divider',
    bottomOuterDivider && 'has-bottom-divider',
    hasBgColor && 'has-bg-color',
    invertColor && 'invert-color',
    className
  );

  const innerClasses = classNames(
    'features-split-inner section-inner',
    topDivider && 'has-top-divider',
    bottomDivider && 'has-bottom-divider'
  );

  const splitClasses = classNames(
    'split-wrap',
    invertMobile && 'invert-mobile',
    invertDesktop && 'invert-desktop',
    alignTop && 'align-top'
  );

  const sectionHeader = {
    title: 'AI-Powered Manufacturing for the Future',
    paragraph: 'Our intelligent digital twin technology optimizes the EV battery module assembly process. Real-time predictions, process improvements, and data-driven insights help you reduce failures, boost efficiency, and maximize sustainability in manufacturing.'
  };

  return (
    <section
      {...props}
      className={outerClasses}
      style={{ backgroundColor: '#1E2746', color: '#A1FFC4' }}  // Dark Blue background with light green text
    >
      <div className="container">
        <div className={innerClasses}>
          <SectionHeader data={sectionHeader} className="center-content" />
          <div className={splitClasses}>

            <div className="split-item">
              <div className="split-item-content center-content-mobile reveal-from-left" data-reveal-container=".split-item">
                <div className="text-xxs text-color-primary fw-600 tt-u mb-8">
                  Optimized Performance
                </div>
                <h3 className="mt-0 mb-12" style={{ color: '#fff' }}>
                  Real-Time Process Insights
                </h3>
                <p className="m-0" style={{ color: '#A1FFC4' }}>
                  Our AI-driven model predicts component failures, recommends process optimizations, and provides real-time insights that dynamically improve production efficiency and reduce waste.
                </p>
              </div>
              <div className={
                classNames(
                  'split-item-image center-content-mobile reveal-from-bottom',
                  imageFill && 'split-item-image-fill'
                )}
                data-reveal-container=".split-item">
                <Image
                  src={require('./../../assets/images/features-split-image-01.png')}
                  alt="AI Manufacturing Process"
                  width={528}
                  height={396} />
              </div>
            </div>

            {/* Repeat similar structure for other split items */}

          </div>
        </div>
      </div>
    </section>
  );
}

FeaturesSplit.propTypes = propTypes;
FeaturesSplit.defaultProps = defaultProps;

export default FeaturesSplit;